# Runtime dependencies are declared in pyproject.toml.
# This file is kept for compatibility with the Actions workflow's
# `pip install -r requirements.txt` step. Pin upper bounds to avoid
# surprise breakage from major version bumps.
zulip>=0.9.0,<1.0
trio>=0.22.0,<1.0
PyYAML>=6.0,<7.0
